apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ allow_trafico_con_multiple_selectors_app_name }}
spec:
  podSelector:
    matchLabels: 
      {{ allow_trafico_con_multiple_selectors_app_matchlabels | to_nice_yaml(indent=2) | trim | indent(6)  }}
  ingress:
  - from: 
    {{ allow_trafico_solo_un_puerto_app_podselector | to_nice_yaml(indent=2)| indent(4) }}